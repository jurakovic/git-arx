# CLAUDE.md

`git-arx` is a single self-contained bash script with no build step.

For implementation details, design decisions, testing, and versioning procedures see [INTERNALS.md](INTERNALS.md).
For end-user documentation see [README.md](README.md).

## When commands or options change

- Update the relevant sections in `README.md` and `INTERNALS.md` if applicable
- Keep `git-arx-completion.bash` in sync

## Testing

```bash
bash test.sh
```
